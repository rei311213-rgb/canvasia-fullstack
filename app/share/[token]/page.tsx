import Link from 'next/link';
import { notFound } from 'next/navigation';
import { prisma } from '@/lib/prisma';
export default async function SharedPage({params}:{params:Promise<{token:string}>}){const {token}=await params;const share=await prisma.projectShare.findUnique({where:{token},include:{project:true}});if(!share||!share.enabled)notFound();const data:any=share.project.data;const objects=Array.isArray(data?.objects)?data.objects:[];const background=data?.background||'#ffffff';return <main className="sharePage"><header><b>Canvasia</b><span>View-only shared design</span><Link href="/login">Sign in</Link></header><section><h1>{share.project.name}</h1><p className="muted">{share.project.type}</p><div className="sharedCanvas" style={{background}}>{objects.map((o:any)=><div key={o.id} className={`obj ${o.kind}`} style={{left:o.x,top:o.y,width:o.w,height:o.h,fontSize:o.fontSize,opacity:o.opacity,transform:`rotate(${o.rotation||0}deg)`,fontFamily:o.fontFamily,fontWeight:o.fontWeight,textAlign:o.textAlign,lineHeight:o.lineHeight,letterSpacing:o.letterSpacing,color:o.color,backgroundColor:o.kind!=='text'&&o.kind!=='icon'&&!o.gradient?o.color:undefined,backgroundImage:o.kind==='image'&&o.src?`url(${o.src})`:o.gradient?`linear-gradient(135deg, ${o.color||'#5b3df5'}, ${o.gradientColor||'#9b8cff'})`:undefined,textDecoration:o.textDecoration,boxShadow:o.shadow?'0 8px 18px rgba(0,0,0,.22)':undefined}}>{o.kind==='image'?null:o.text}</div>)}</div></section></main>}
